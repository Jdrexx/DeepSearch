# Deep Search Toolkit — runtime dependencies only.
# (The previous requirements.txt was a full machine pip-freeze that dragged in
# unrelated packages — incl. hermes-agent with known vulns. Real deps only here.)
ddgs>=9.14
requests[socks]>=2.31        # socks extra pulls PySocks for the Tor SOCKS5 proxy
beautifulsoup4>=4.12
rich>=13
